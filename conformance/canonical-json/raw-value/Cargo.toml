[package]
name = "recourse-raw-value-consumer"
version = "0.0.0"
edition = "2024"
rust-version = "1.96"
publish = false

[dependencies]
recourse = { path = "../../../crates/recourse" }
schemars = "1.0"
serde = "1.0"
serde_json = { version = "1.0", features = ["raw_value"] }
