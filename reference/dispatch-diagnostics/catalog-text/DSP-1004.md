# Idempotency key conflict

The idempotency key is already bound to a job created from different inputs. Dispatch preserves that first binding so retries cannot create divergent jobs.

Repeat the original request unchanged to read its result, or use a new idempotency key for different inputs. The evidence identifies the original job.
