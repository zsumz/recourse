# Unsupported media type

The operation accepts JSON, but the request declared another representation. Dispatch rejects the representation before interpreting it as application input.

Encode the request body as JSON and send `Content-Type: application/json`.
