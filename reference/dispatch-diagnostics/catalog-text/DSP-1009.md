# Job dispatch failed

Dispatch accepted the job, but a background dispatch attempt failed before the job completed. This durable diagnostic records the failed attempt separately from the successful HTTP acceptance response.

Inspect the public impact before retrying. The impact states whether the destination changed and whether any durable artifacts were created, so reconciliation does not depend on private worker error text.
