# Job queue unavailable

Dispatch cannot currently reach the durable job queue. The same permanent identity can reject a request with `Retry-After` and describe the current service state as a health finding.

Check queue connectivity, credentials, and network policy. Treat the consecutive failure count as public evidence of the observed condition, not as a retry policy.
