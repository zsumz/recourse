# Unsupported method

The selected resource does not implement the request method. The required `Allow` response header is constructed from typed method values and is the authority for supported alternatives.

Repeat the request using one of the methods listed by `Allow`.
