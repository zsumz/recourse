# Service temporarily unavailable

Dispatch cannot complete the request because a transient dependency or capacity condition is active. The required `Retry-After` response header supplies the minimum known delay.

Wait for that delay and retry with bounded exponential backoff.
